from django.contrib import admin
from django.urls import path
from . import views

app_name = "transactions"

urlpatterns = [
    # path('', views.get_nasdaq_symbols, name='stock-symbols'),
]
